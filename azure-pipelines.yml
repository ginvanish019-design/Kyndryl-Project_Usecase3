trigger:
  branches:
    include:
      - main

pool:
  name: 'Anish_Agent'  # Your self-hosted agent

steps:
- checkout: self

# 🐍 Verify Python installation
- bash: |
    set -e
    echo "🐍 Checking Python version..."
    python3 --version
    echo "✅ Python is installed."
  displayName: "Verify Python Installation"

# 📦 Install required Azure SDK dependencies
- bash: |
    set -e
    echo "📦 Installing Azure SDK dependencies..."
    python3 -m pip install --upgrade pip
    python3 -m pip install azure-identity azure-mgmt-compute azure-monitor-query
    echo "🔍 Verifying Azure Monitor package installation..."
    python3 -m pip show azure-monitor-query
    echo "✅ Azure SDK dependencies installed successfully."
  displayName: "Install Azure SDK Dependencies"

# 🚀 Run Multi-Cloud Auto Manager
- bash: |
    set -e
    echo "🚀 Running Multi-Cloud Auto Manager..."
    python3 multi_cloud_auto_manager.py
    echo "✅ Multi-Cloud Auto Manager completed successfully."
  displayName: "Run Multi-Cloud Auto Manager"
